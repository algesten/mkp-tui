[package]
name = "mkpclient-state-queue"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true

# External-fact source: the server's playback queue, reconstructed
# from broadcast `QueueDelta` / `QueueCatchUp` frames.

[dependencies]
imbl = { workspace = true }
mkproto = { workspace = true }
