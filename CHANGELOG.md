# Unreleased

* The client reconnects on its own after the connection drops, so a
  sleeping laptop or a brief network outage no longer throws you out
  to the server list
* Playlists, the track list and the queue stay on screen while the
  client reconnects, behind a modal, instead of being cleared
* Picking a server in the switch dialog is enough to switch to it —
  it no longer drops out to the full-screen server list or reports
  the change as a lost connection

# 1.0.0

* First version
