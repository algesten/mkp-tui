# Unreleased

* Keep viewed playlists fresh and apply concurrent edits without clearing
  another client's pending changes
* Reconnect on its own after the connection drops, and come back to the
  same server and the same view instead of the server list

# 1.0.0

* First version
